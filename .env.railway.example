# Railway MySQL public TCP endpoint — copy this to .env.railway and fill in values.
# Get values from Railway dashboard → MySQL service → Variables.
# .env.railway is gitignored and must never be committed.
MYSQL_HOST=nozomi.proxy.rlwy.net
MYSQL_PORT=41048
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB=
